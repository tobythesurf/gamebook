h1 ゲームブック一覧

- if current_user
  .nav.justify-content-end
    = link_to '新規作成', new_book_path, class: 'btn btn-primary'

table.table.table-hover
  thead.thead-default
    tr
      th = '題名'
      th = '大体の内容'
      th
  tbody
    - @books.each do |book|
      tr
        td = book.book_title
        td = book.description
        - if current_user
          td
            = link_to '閲覧', book, class: 'btn btn-outline-primary mr-3'
            = link_to '編集', edit_book_path(book), class: 'btn btn-primary mr-3'
            = link_to '削除', book, method: :delete, data: { confirm: "#{book.book_title}を削除します。よろしいですか？"}, class: 'btn btn-danger'